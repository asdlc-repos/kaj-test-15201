apiVersion: openchoreo.dev/v1alpha1
metadata:
  name: todo-web

endpoints:
  - name: http
    type: HTTP
    port: 9090
    visibility:
      - external

dependencies:
  endpoints:
    - component: todo-api
      name: todo-api-http
      visibility: external
      envBindings:
        address: TODO_API_URL
