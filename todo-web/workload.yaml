apiVersion: openchoreo.dev/v1alpha1
metadata:
  name: todo-web

endpoints:
  - name: http
    type: HTTP
    port: 9090
    visibility:
      - external

dependencies:
  endpoints:
    - component: kaj-test-15-todo-api
      name: todo-api-http
      visibility: project
      envBindings:
        address: TODO_API_URL
