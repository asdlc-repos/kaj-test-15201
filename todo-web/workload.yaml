apiVersion: openchoreo.dev/v1alpha1
kind: Workload
spec:
  endpoints:
    http:
      type: HTTP
      port: 9090
      visibility:
        - external
  dependencies:
    endpoints:
      - component: todo-api
        name: http
        visibility: project
        envBindings:
          address: TODO_API_URL
