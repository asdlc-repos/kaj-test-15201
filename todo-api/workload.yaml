apiVersion: openchoreo.dev/v1alpha1
metadata:
  name: todo-api

endpoints:
  - name: todo-api-http
    type: REST
    port: 9090
    basePath: /
    visibility:
      - project
      - namespace
      - internal
      - external
